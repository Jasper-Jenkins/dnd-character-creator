import React from 'react'

const CharacterClass = props => {
    console.log("Character Class properties", props.classProps)
    //let classTraits = Object.keys(props.classProps.classSelected);
    //switch (classTraits) {
    //    case 'name':
            
    //}




    //console.log(classTraits)
    //for (var k = 0; k < classTraits.length; k++) {
    //    switch (classTraits)


    //}
    //let classTraitsDisplay = classTraits.map((trait, index) => {

    //    return (<li key={index}>{props.classSelected[trait]}</li>);

    //});
    //// console.log(classTraitsDisplay)
    //return (<div className="col-12 info">
    //    <ul>
    //        <li>working on it</li>
    //    </ul>
    //</div>);
    return (<div className="col-12 info">
                <p>working on class again</p>
            </div>);


}

export default CharacterClass